using Plarium.Assets.GameCore.Events;
using UnityEngine;
using VContainer;

namespace Plarium.Assets.GameCore
{
    public class UnityEventTracker : MonoBehaviour
    {
        /*private EventBus _eventBus;

        [Inject]
        private void Construct(EventBus eventBus)
        {
            _eventBus = eventBus;
        }

        private void OnApplicationQuit()
        {
            _eventBus.Publish(GameplayEvent.ApplicationQuit, BaseEventParams.Empty);
        }*/
    }
}
